<?php

declare(strict_types=1);

/**
 * Web routes (HTML pages, redirects, etc.)
 *
 * This skeleton is API-only. Add web routes here if needed:
 *
 * $app->router->get('/dashboard', [DashboardController::class, 'index']);
 */
